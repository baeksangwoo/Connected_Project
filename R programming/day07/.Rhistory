library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다 메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1)
{
p<-PlotOnStaticMap(mymap,
lat=kt$lon,
lon=kt$lat,
destfile = 'wifi_point.jpg',
cex=0.3,pch=10,col='red'
);
}ifelse(a==2,
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'wifi_point.jpg',
cex=0.3,pch=10,col='red'
),p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'wifi_point.jpg',
cex=0.3,pch=10,col='red'
))
print(p);
dev.off();
}
wifi(1)
library(KoNLP)
library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다
#메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
data3
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
lg
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1){
p<-PlotOnStaticMap(mymap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=0.3,pch=10,col='red')
}
ifelse(a==2,
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'skt_point.jpg',
cex=0.3,pch=10,col='red'
),
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'lg_point.jpg',
cex=0.3,pch=10,col='red'
))
wifi<-function(a)
{
library(KoNLP)
library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다
#메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
data3
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1){
p<-PlotOnStaticMap(mymap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=0.3,pch=10,col='red')
}
ifelse(a==2,
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'skt_point.jpg',
cex=0.3,pch=10,col='red'
),
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'lg_point.jpg',
cex=0.3,pch=10,col='red'
))
print(p);
dev.off();
}
wifi(1)
wifi(3)
#데이터를 알아볼수 있게 rename
wifi<-function(a)
{
library(KoNLP)
library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다
#메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
data3
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1){
p<-PlotOnStaticMap(mymap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=0.3,pch=10,col='red')
}
ifelse(a==2,
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'skt_point.jpg',
cex=0.3,pch=10,col='red'
),
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'lg_point.jpg',
cex=0.3,pch=10,col='red'
))
print(p);
dev.off();
}
#데이터를 알아볼수 있게 rename
wifi<-function(a)
{
library(KoNLP)
library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
mycenter=c(37.501001, 127.038787)
mymap<-GetMap(
center = mycenter,
zoom=10,
maptype = 'road',
destfile = 'mymap.jpg'
)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다
#메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
data3
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1){
p<-PlotOnStaticMap(mymap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=0.3,pch=10,col='red')
}
ifelse(a==2,
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'skt_point.jpg',
cex=0.3,pch=10,col='red'
),
p<-PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'lg_point.jpg',
cex=0.3,pch=10,col='red'
))
print(p);
dev.off();
}
wifi(1)
#데이터를 알아볼수 있게 rename
wifi<-function(a)
{
library(KoNLP)
library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
mycenter=c(37.501001, 127.038787)
ktmap<-GetMap(
center = mycenter,
zoom=8,
maptype = 'road',
destfile = 'ktwifi.jpg'
)
sktmap<-GetMap(
center = mycenter,
zoom=8,
maptype = 'road',
destfile = 'sktwifi.jpg'
)
lgmap<-GetMap(
center = mycenter,
zoom=8,
maptype = 'road',
destfile = 'lgwifi.jpg'
)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다
#메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
data3
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1){
p<-PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=0.3,pch=10,col='red')
}
ifelse(a==2,
p<-PlotOnStaticMap(sktmap,
lat=data3$lon,
lon=data3$lat,
destfile = 'skt_point.jpg',
cex=0.3,pch=10,col='red'
),
p<-PlotOnStaticMap(lgmap,
lat=data3$lon,
lon=data3$lat,
destfile = 'lg_point.jpg',
cex=0.3,pch=10,col='red'
))
print(p);
dev.off();
}
wifi(1)
#데이터를 알아볼수 있게 rename
wifi<-function(a)
{
library(KoNLP)
library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
mycenter=c(37.501001, 127.038787)
ktmap<-GetMap(
center = mycenter,
zoom=8,
maptype = 'road',
destfile = 'ktwifi.jpg'
)
sktmap<-GetMap(
center = mycenter,
zoom=8,
maptype = 'road',
destfile = 'sktwifi.jpg'
)
lgmap<-GetMap(
center = mycenter,
zoom=8,
maptype = 'road',
destfile = 'lgwifi.jpg'
)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다
#메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
data3
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1){
p<-PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=1,pch=10,col='blue')
}
ifelse(a==2,
p<-PlotOnStaticMap(sktmap,
lat=data3$lon,
lon=data3$lat,
destfile = 'skt_point.jpg',
cex=1,pch=10,col='red'
),
p<-PlotOnStaticMap(lgmap,
lat=data3$lon,
lon=data3$lat,
destfile = 'lg_point.jpg',
cex=1,pch=10,col='green'
))
print(p);
dev.off();
}
wifi(3)
#데이터를 알아볼수 있게 rename
wifi<-function(a)
{
library(KoNLP)
library(dplyr)
library(stringr)
library(ggplot2)
library(RgoogleMaps)
mycenter=c(37.501001, 127.038787)
ktmap<-GetMap(
center = mycenter,
zoom=10,
maptype = 'road',
destfile = 'ktwifi.jpg'
)
sktmap<-GetMap(
center = mycenter,
zoom=10,
maptype = 'road',
destfile = 'sktwifi.jpg'
)
lgmap<-GetMap(
center = mycenter,
zoom=10,
maptype = 'road',
destfile = 'lgwifi.jpg'
)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다
#메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
data3
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
if(a==1){
p<-PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=1,pch=10,col='blue')
}
ifelse(a==2,
p<-PlotOnStaticMap(sktmap,
lat=data3$lon,
lon=data3$lat,
destfile = 'skt_point.jpg',
cex=1,pch=10,col='red'
),
p<-PlotOnStaticMap(lgmap,
lat=data3$lon,
lon=data3$lat,
destfile = 'lg_point.jpg',
cex=1,pch=10,col='green'
))
print(p);
dev.off();
}
wifi(1)
PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=1,pch=10,col='blue')
ktmap<-GetMap(
center = mycenter,
zoom=10,
maptype = 'road',
destfile = 'ktwifi.jpg'
)
ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=1,pch=10,col='blue')
mycenter=c(37.501001, 127.038787)
ktmap<-GetMap(
center = mycenter,
zoom=10,
maptype = 'road',
destfile = 'ktwifi.jpg'
)
PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=1,pch=10,col='blue')
kt<-data3 %>% filter(company=='KT')
skt<-data3 %>% filter(company=='SKT')
lg<-data3 %>% filter(company=='LGU+')
PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=1,pch=10,col='blue')
PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'kt_point.jpg',
cex=1,pch=10,col='red')
PlotOnStaticMap(ktmap,
lat=kt$lon,
lon=kt$lat,
destfile = 'mymap_point.jpg',
cex=1,pch=10,col='red')
#install.packages('RgoogleMaps')
library(RgoogleMaps)
mycenter=c(37.501001, 127.038787)
mymap<-GetMap(
center = mycenter,
zoom=10,
maptype = 'road',
destfile = 'mymap.jpg'
)
jpeg(filename = "mypoint.jpg",width=680, height=680, quality=100);
mydata<- data.frame(
a=c(1:5),
lat=c(37.501401,37.501201,37.501101, 37.501601, 37.503001),
lon=c(126.031787 , 127.032787, 127.138787, 127.038737, 127.018787)            );
p<-PlotOnStaticMap(mymap,
lat=mydata$lat,
lon = mydata$lon,
destfile = 'mymap_point.jpg',
cex=1,pch=10,col='red'
)
print(p);
dev.off()
library(dplyr)
mycenter=c(37.501001, 127.038787)
mymap<-GetMap(
center = mycenter,
zoom=11,
maptype = 'road',
destfile = 'wifi.jpg'
)
svd<-read.csv('seoul.csv', header = T)
#데이터를 불러 올때 유니코드로 불러오거나 UTF-8로 불러온다 메모장을 이용
data3<-rename(svd,
company=설치기관.회사.,
lat=설치위치.Y좌표.,
lon=설치위치.X좌표.
)
#데이터를 알아볼수 있게 rename
kt<-data3 %>% filter(company=='KT')
kt
skt<-data3 %>% filter(company=='SKT')
PlotOnStaticMap(mymap,
lat=kt$lon,
lon=kt$lat,
destfile = 'wifi_point.jpg',
cex=0.3,pch=10,col='red'
)
PlotOnStaticMap(mymap,
lat=data3$lon,
lon=data3$lat,
destfile = 'wifi_point.jpg',
cex=0.3,pch=10,col='red'
)
